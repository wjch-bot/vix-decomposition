# Memory - 2026-03-26

## VIX Decomposition Project

### Status: Debugging complete — DTE logic confirmed correct; CSV was stale

### Key Corrections (Mar 26 evening session)

1. **Near-term definition (CRITICAL)**:
   - Near-term = **last expiry with DTE ≤ 30** (closest to 30 from below)
   - Far-term = **first expiry with DTE > 30** (closest to 30 from above)
   - Algorithm in `find_nearest_expiries()` was ALREADY CORRECT
   - The stale CSV (showing 1/2 DTE) was from an earlier buggy version

2. **Actual 2026 DTE pairs** (near/far):
   - Near-term DTE range: 14–30 days
   - Far-term DTE range: 32–46 days
   - Always properly brackets 30 DTE (good for VIX interpolation)
   - No 1-DTE near-term issues — this was a stale-data confusion

3. **Files moved**: `vix_decomposition.py` and `VIX_DECOMPOSITION.md` moved to `VIX-project/` subdirectory (repo root versions deleted)

4. **F1 formula**: Still being debugged — see vix_analysis.py `run_decomposition()` function

### Files
- `VIX-project/vix_analysis.py` — main analysis (fetch, VIX compute, decompose)
- `VIX-project/vix_decomposition.py` — formula reference + validation
- `VIX-project/VIX_DECOMPOSITION.md` — whitepaper notes
- `VIX-project/output/` — CSV results and charts

### GitHub
- Working in `wjch-bot` branch
- Committing all VIX-project files per user's auxiliary-file rule
